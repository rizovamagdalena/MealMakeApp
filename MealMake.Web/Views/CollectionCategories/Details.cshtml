@model MealMake.Domain.Domain_Models.CollectionCategory

@{
    ViewData["Title"] = "Category Details";
}

<div class="container my-5">
    <h2>Category Details</h2>
    <hr />

    <div class="card shadow-sm p-3 mb-4">
        <h4>@Model.Name</h4>
        <dl class="row mt-3">
            <dt class="col-sm-3">Name</dt>
            <dd class="col-sm-9">@Model.Name</dd>
        </dl>
    </div>

    <div>
        <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-primary me-2">✏️ Edit</a>
        <a asp-action="Index" class="btn btn-secondary">⬅️ Back to List</a>
    </div>
</div>

@* @model MealMake.Domain.Domain_Models.CollectionCategory

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>CollectionCategory</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div> *@
